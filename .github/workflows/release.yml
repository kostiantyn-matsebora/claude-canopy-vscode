name: Release
on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.4.1)'
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Resolve tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "value=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.tag.outputs.value }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Verify tag matches package.json version
        run: |
          PKG=$(node -p "require('./package.json').version")
          VER="${{ steps.tag.outputs.value }}"
          VER="${VER#v}"
          if [ "$PKG" != "$VER" ]; then
            echo "ERROR: package.json version ($PKG) != tag ${{ steps.tag.outputs.value }}"
            exit 1
          fi

      - run: npm run compile
      - run: npm test
      - run: npm run package

      - name: Create GitHub Release
        run: |
          gh release create "${{ steps.tag.outputs.value }}" *.vsix \
            --title "Canopy Skills ${{ steps.tag.outputs.value }}" \
            --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
