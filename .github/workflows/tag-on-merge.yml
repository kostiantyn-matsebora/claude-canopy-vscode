name: Tag on Merge

on:
  push:
    branches: [master]

jobs:
  tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read version from package.json
        id: version
        run: echo "value=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Check if tag already exists on remote
        id: tag_check
        run: |
          TAG="v${{ steps.version.outputs.value }}"
          if git ls-remote --tags origin "refs/tags/$TAG" | grep -q "$TAG"; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create and push tag
        if: steps.tag_check.outputs.exists == 'false'
        id: create_tag
        run: |
          TAG="v${{ steps.version.outputs.value }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "$TAG"
          git push https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }} "$TAG"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Dispatch release workflow
        if: steps.create_tag.outputs.tag != ''
        run: |
          gh workflow run release.yml --ref "${{ steps.create_tag.outputs.tag }}" \
            --field tag="${{ steps.create_tag.outputs.tag }}"
        env:
          GH_TOKEN: ${{ secrets.PAT }}
