{
  "New Skill": {
    "prefix": "skill",
    "description": "New Canopy skill skeleton (agentskills.io layout — compatibility + safety preamble + metadata.argument-hint + canopy-features manifest)",
    "body": [
      "---",
      "name: ${1:skill-name}",
      "description: ${2:One-line description.}",
      "compatibility: Requires the canopy-runtime skill (published at github.com/kostiantyn-matsebora/claude-canopy). Install with any agentskills.io-compatible tool — e.g. `gh skill install`, `git clone`, the repo's `install.sh`/`install.ps1`, or the Claude Code plugin marketplace. Supports Claude Code and GitHub Copilot.",
      "metadata:",
      "  argument-hint: \"${3:<required-arg>}\"",
      "  canopy-features: [interaction, verify]",
      "---",
      "",
      "> **Runtime required.** This skill uses Canopy tree notation; canopy-runtime must be active.",
      ">",
      "> **Detect canopy-runtime** — present if either:",
      "> - `canopy-runtime/SKILL.md` exists under `.claude/skills/`, `.github/skills/`, or `.agents/skills/`, OR",
      "> - a canopy-runtime marker block exists in `CLAUDE.md` or `.github/copilot-instructions.md`.",
      ">",
      "> **If neither is present** — install canopy-runtime first (see the `compatibility` field for the source and install options), then re-invoke this skill.",
      ">",
      "> Do not interpret the `## Tree` without canopy-runtime active.",
      "",
      "${4:Parse \\$ARGUMENTS.}",
      "",
      "---",
      "",
      "## Tree",
      "",
      "* ${1:skill-name}",
      "  * SHOW_PLAN >> ${5:what will change}",
      "  * ASK << Proceed? | Yes | No",
      "  * ${6:OP_NAME << input >> output}",
      "  * VERIFY_EXPECTED << assets/verify/verify-expected.md",
      "",
      "## Rules",
      "",
      "- ${7:Add invariants here.}",
      "",
      "## Response: ${8:summary | details}"
    ]
  },
  "New Skill with Agent": {
    "prefix": "skill-explore",
    "description": "New Canopy skill with explore subagent (agentskills.io layout — includes canopy-features manifest)",
    "body": [
      "---",
      "name: ${1:skill-name}",
      "description: ${2:One-line description.}",
      "compatibility: Requires the canopy-runtime skill (published at github.com/kostiantyn-matsebora/claude-canopy). Install with any agentskills.io-compatible tool — e.g. `gh skill install`, `git clone`, the repo's `install.sh`/`install.ps1`, or the Claude Code plugin marketplace. Supports Claude Code and GitHub Copilot.",
      "metadata:",
      "  argument-hint: \"${3:<required-arg>}\"",
      "  canopy-features: [interaction, explore, verify]",
      "---",
      "",
      "> **Runtime required.** This skill uses Canopy tree notation; canopy-runtime must be active.",
      ">",
      "> **Detect canopy-runtime** — present if either:",
      "> - `canopy-runtime/SKILL.md` exists under `.claude/skills/`, `.github/skills/`, or `.agents/skills/`, OR",
      "> - a canopy-runtime marker block exists in `CLAUDE.md` or `.github/copilot-instructions.md`.",
      ">",
      "> **If neither is present** — install canopy-runtime first (see the `compatibility` field for the source and install options), then re-invoke this skill.",
      ">",
      "> Do not interpret the `## Tree` without canopy-runtime active.",
      "",
      "${4:Parse \\$ARGUMENTS.}",
      "",
      "---",
      "",
      "## Agent",
      "",
      "**explore** — ${5:reads the project root for relevant files.} Output contract: `assets/schemas/explore-schema.json`.",
      "",
      "---",
      "",
      "## Tree",
      "",
      "* ${1:skill-name}",
      "  * EXPLORE >> context",
      "  * SHOW_PLAN >> ${6:what will change}",
      "  * ASK << Proceed? | Yes | No",
      "  * ${7:OP_NAME << context >> output}",
      "  * VERIFY_EXPECTED << assets/verify/verify-expected.md",
      "",
      "## Rules",
      "",
      "- ${8:Add invariants here.}",
      "",
      "## Response: ${9:summary | details}"
    ]
  },
  "Op Definition": {
    "prefix": "op",
    "description": "New op definition for ops.md",
    "body": [
      "## ${1:OP_NAME} << ${2:input} >> ${3:output}",
      "",
      "${4:Description of what this op does.}"
    ]
  },
  "Subagent Op Definition": {
    "prefix": "op-subagent",
    "description": "New subagent op definition (S2 — marker dispatch)",
    "body": [
      "## ${1:OP_NAME} << ${2:input} >> ${3:output}",
      "",
      "> **Subagent.** Output contract: `assets/schemas/${4:${1/(.*)/${1:/downcase}/}}-output.json`.",
      "",
      "${5:Description of what this subagent op does.}",
      "",
      "* ${1:OP_NAME} << ${2:input} >> ${3:output}",
      "  * ${6:Step using only << inputs and static skill assets}"
    ]
  },
  "Subagent Call (bold)": {
    "prefix": "call-subagent",
    "description": "Subagent dispatch call-site — bold-wrapped op name",
    "body": [
      "* **${1:OP_NAME}** << ${2:input} >> ${3:output}"
    ]
  },
  "Op Definition with Tree": {
    "prefix": "op-tree",
    "description": "New op definition with tree body",
    "body": [
      "## ${1:OP_NAME} << ${2:input} >> ${3:output}",
      "",
      "* ${1:OP_NAME} << ${2:input} >> ${3:output}",
      "  * ${4:OP_STEP_ONE << input}",
      "  * ${5:OP_STEP_TWO << input}"
    ]
  },
  "IF / ELSE": {
    "prefix": "if",
    "description": "IF / ELSE branch",
    "body": [
      "* IF << ${1:condition}",
      "  * ${2:action if true}",
      "* ELSE",
      "  * ${3:action if false}"
    ]
  },
  "IF / ELSE_IF / ELSE": {
    "prefix": "ifelse",
    "description": "IF / ELSE_IF / ELSE chain",
    "body": [
      "* IF << ${1:condition}",
      "  * ${2:action if true}",
      "* ELSE_IF << ${3:condition2}",
      "  * ${4:action if condition2}",
      "* ELSE",
      "  * ${5:action otherwise}"
    ]
  },
  "ASK": {
    "prefix": "ask",
    "description": "ASK primitive — prompt user with options",
    "body": [
      "* ASK << ${1:Question?} | ${2:Yes} | ${3:No}"
    ]
  },
  "SHOW_PLAN": {
    "prefix": "plan",
    "description": "SHOW_PLAN primitive — display structured plan",
    "body": [
      "* SHOW_PLAN >> ${1:field1} | ${2:field2} | ${3:field3}"
    ]
  },
  "VERIFY_EXPECTED": {
    "prefix": "verify",
    "description": "VERIFY_EXPECTED primitive (agentskills layout — assets/verify/)",
    "body": [
      "* VERIFY_EXPECTED << assets/verify/${1:verify-expected}.md"
    ]
  },
  "END with message": {
    "prefix": "end",
    "description": "END primitive — halt skill with message",
    "body": [
      "* END ${1:Reason skill halted.}"
    ]
  },
  "Read resource": {
    "prefix": "read",
    "description": "Load a category resource file",
    "body": [
      "* Read `${1|assets/schemas,assets/templates,assets/constants,assets/policies,assets/verify,assets/checklists,scripts,references,schemas,templates,commands,constants,policies,verify,checklists|}/${2:file}` for ${3:purpose}"
    ]
  },
  "SWITCH / CASE / DEFAULT": {
    "prefix": "switch",
    "description": "SWITCH / CASE / DEFAULT block — branch on a single expression",
    "body": [
      "* SWITCH << ${1:expression}",
      "  * CASE << ${2:value1}",
      "    * ${3:action for value1}",
      "  * CASE << ${4:value2}",
      "    * ${5:action for value2}",
      "  * DEFAULT",
      "    * ${6:action otherwise}"
    ]
  },
  "FOR_EACH": {
    "prefix": "for",
    "description": "FOR_EACH loop — iterate over a collection",
    "body": [
      "* FOR_EACH << ${1:item} in ${2:collection}",
      "  * ${3:body action using <<${1:item}>>}"
    ]
  },
  "FOR_EACH with BREAK": {
    "prefix": "for-break",
    "description": "FOR_EACH loop with conditional BREAK",
    "body": [
      "* FOR_EACH << ${1:item} in ${2:collection}",
      "  * ${3:body action}",
      "  * IF << ${4:exit-condition}",
      "    * BREAK"
    ]
  },
  "PARALLEL": {
    "prefix": "parallel",
    "description": "PARALLEL block — heterogeneous fan-out across N subagents",
    "body": [
      "* PARALLEL",
      "  * ${1:first subagent invocation} >> ${2:ctx_a}",
      "  * ${3:second subagent invocation} >> ${4:ctx_b}"
    ]
  },
  "bind variable": {
    "prefix": "bind",
    "description": "Bind a context variable",
    "body": [
      "* bind ${1:variable} = ${2:value}"
    ]
  }
}
