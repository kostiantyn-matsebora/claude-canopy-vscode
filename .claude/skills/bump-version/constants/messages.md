# Messages

## no_changes
No changes detected since the last changelog entry. Run `/update` first to pull the latest Canopy version and apply any pending implementation changes, then re-run `/bump-version`.
